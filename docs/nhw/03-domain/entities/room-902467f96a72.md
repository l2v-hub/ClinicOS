---
id: "entity.room"
kind: "domain-entity"
title: "Room"
status: "inferred"
summary: "Business entity persisted by the Room Prisma model."
bounded_contexts:
  - "context.facility-occupancy"
sources:
  - path: "prisma/schema.prisma"
    symbol: "Room"
    line_start: "330"
    line_end: "341"
    confidence: "observed"
relations:
  - type: "belongs-to"
    target: "context.facility-occupancy"
    evidence: "prisma/schema.prisma"
    confidence: "inferred"
  - type: "persists-as"
    target: "data.model.room"
    evidence: "prisma/schema.prisma"
    confidence: "inferred"
tags:
  - "domain-entity"
  - "room"
last_verified:
  commit: "working-tree"
  inventory_hash: "72e609c2ac06a4ade4df3d8719628805e2d1606ce4aec0e6f662451324869f8e"
inference_rule: "Business entity reconstructed from the current Prisma model and its executable consumers."
---

## Question Answered

What does `entity.room` represent in ClinicOS?

## Canonical Definition

entity.room is the canonical domain-entity named Room.

## Inputs

- `id: String` (id, required, default=cuid())
- `numero: String` (unique, required)
- `tipo: RoomType` (required, default=singola)
- `piano: String` (required, default="")
- `reparto: String` (required, default="")
- `stato: RoomStatus` (required, default=attiva)
- `note: String` (required, default="")
- `createdAt: DateTime` (required, default=now())
- `updatedAt: DateTime` (required)
- `beds: Bed[]` (required, list)

## Outputs

Lifecycle state persisted as `data.model.room`.

## Dependencies

- - `beds` → `Bed` (many; onDelete=unspecified)

## Side Effects

Creation, mutation, and deletion alter persistent clinical state.

## Consumers

Endpoint and UI consumers are navigable through graph relations to the persistence model.

## Invariants

Identity, nullability, uniqueness, defaults, and relations are authoritative in `prisma/schema.prisma`.

## Failure Modes

Invalid transitions are rejected by API validation or persistence constraints; uncovered business ambiguity is not invented.

## Evidence

- `prisma/schema.prisma:330-341` — Room

## Related Knowledge

- `belongs-to` → `context.facility-occupancy`
- `persists-as` → `data.model.room`
