---
id: 'api.consumer.frontend.frontend.src.components.operator.cartella.diariopazientetab.tsx.284.36'
kind: 'frontend-api-consumer'
title: 'confirmDelete DELETE /patients/${pazienteId}/diary/${entry.id}'
status: 'observed'
summary: 'Frontend request issued by confirmDelete.'
bounded_contexts:
  - 'context.patient-registry'
sources:
  - path: 'frontend/src/components/operator/cartella/DiarioPazienteTab.tsx'
    symbol: 'confirmDelete'
    line_start: '284'
    line_end: '286'
    confidence: 'observed'
relations:
  - type: 'belongs-to'
    target: 'project.frontend'
    evidence: 'frontend/src/components/operator/cartella/DiarioPazienteTab.tsx'
    confidence: 'observed'
tags:
  - 'frontend'
  - 'api-consumer'
last_verified:
  commit: 'working-tree'
  inventory_hash: '88bfed0fcf4eeef0bf5613885ddf3f844a8a14eace6d862aaf9b2a925afb484c'
---

## Question Answered

What does `api.consumer.frontend.frontend.src.components.operator.cartella.diariopazientetab.tsx.284.36` represent in ClinicOS?

## Canonical Definition

api.consumer.frontend.frontend.src.components.operator.cartella.diariopazientetab.tsx.284.36 is the canonical frontend-api-consumer named confirmDelete DELETE /patients/${pazienteId}/diary/${entry.id}.

## Inputs

HTTP method: `DELETE`; path template: `/patients/${pazienteId}/diary/${entry.id}`.

## Outputs

Consumes the backend HTTP response.

## Dependencies

Backend route matching is resolved by method and normalized path.

## Side Effects

Performs a browser-originated HTTP request.

## Consumers

Frontend caller: `confirmDelete`.

## Invariants

The configured API base URL is applied by the frontend request layer.

## Failure Modes

None observed

## Evidence

- `frontend/src/components/operator/cartella/DiarioPazienteTab.tsx:284-286` — confirmDelete

## Related Knowledge

- `belongs-to` → `project.frontend`
