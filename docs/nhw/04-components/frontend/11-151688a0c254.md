---
id: "api.consumer.frontend.frontend.src.app.tsx.597.11"
kind: "frontend-api-consumer"
title: "updateConsegna PUT /consegne/${id}"
status: "observed"
summary: "Frontend request issued by updateConsegna."
bounded_contexts:
  - "context.delivery-quality-governance"
sources:
  - path: "frontend/src/App.tsx"
    symbol: "updateConsegna"
    line_start: "597"
    line_end: "601"
    confidence: "observed"
relations:
  - type: "belongs-to"
    target: "project.frontend"
    evidence: "frontend/src/App.tsx"
    confidence: "observed"
tags:
  - "frontend"
  - "api-consumer"
last_verified:
  commit: "working-tree"
  inventory_hash: "1a33da4292b14f795c966b6b4be3fa1ccc6b48f91cb59e051d8bc5fd29fb86e5"
---

## Question Answered

What does `api.consumer.frontend.frontend.src.app.tsx.597.11` represent in ClinicOS?

## Canonical Definition

api.consumer.frontend.frontend.src.app.tsx.597.11 is the canonical frontend-api-consumer named updateConsegna PUT /consegne/${id}.

## Inputs

HTTP method: `PUT`; path template: `/consegne/${id}`.

## Outputs

Consumes the backend HTTP response.

## Dependencies

Backend route matching is resolved by method and normalized path.

## Side Effects

Performs a browser-originated HTTP request.

## Consumers

Frontend caller: `updateConsegna`.

## Invariants

The configured API base URL is applied by the frontend request layer.

## Failure Modes

None observed

## Evidence

- `frontend/src/App.tsx:597-601` — updateConsegna

## Related Knowledge

- `belongs-to` → `project.frontend`
