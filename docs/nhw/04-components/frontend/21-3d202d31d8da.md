---
id: "api.consumer.frontend.frontend.src.app.tsx.863.21"
kind: "frontend-api-consumer"
title: "updatePaziente PATCH /patients/${id}"
status: "observed"
summary: "Frontend request issued by updatePaziente."
bounded_contexts:
  - "context.patient-registry"
sources:
  - path: "frontend/src/App.tsx"
    symbol: "updatePaziente"
    line_start: "863"
    line_end: "867"
    confidence: "observed"
relations:
  - type: "belongs-to"
    target: "project.frontend"
    evidence: "frontend/src/App.tsx"
    confidence: "observed"
tags:
  - "frontend"
  - "api-consumer"
last_verified:
  commit: "working-tree"
  inventory_hash: "57c381a02126e2007732515f73987664ea86709cad20b363c4f1047427a9bd9c"
---

## Question Answered

What does `api.consumer.frontend.frontend.src.app.tsx.863.21` represent in ClinicOS?

## Canonical Definition

api.consumer.frontend.frontend.src.app.tsx.863.21 is the canonical frontend-api-consumer named updatePaziente PATCH /patients/${id}.

## Inputs

HTTP method: `PATCH`; path template: `/patients/${id}`.

## Outputs

Consumes the backend HTTP response.

## Dependencies

Backend route matching is resolved by method and normalized path.

## Side Effects

Performs a browser-originated HTTP request.

## Consumers

Frontend caller: `updatePaziente`.

## Invariants

The configured API base URL is applied by the frontend request layer.

## Failure Modes

None observed

## Evidence

- `frontend/src/App.tsx:863-867` — updatePaziente

## Related Knowledge

- `belongs-to` → `project.frontend`
