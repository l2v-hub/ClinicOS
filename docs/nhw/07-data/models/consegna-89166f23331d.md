---
id: "data.model.consegna"
kind: "data-model"
title: "Consegna"
status: "observed"
summary: "Prisma persistence model Consegna."
bounded_contexts:
  - "context.operator-collaboration"
sources:
  - path: "prisma/schema.prisma"
    symbol: "Consegna"
    line_start: "377"
    line_end: "394"
    confidence: "observed"
relations:
  - type: "belongs-to"
    target: "project.prisma"
    evidence: "prisma/schema.prisma"
    confidence: "observed"
tags:
  - "prisma"
  - "database-model"
last_verified:
  commit: "working-tree"
  inventory_hash: "2795bded959bb18b7093e7b78e09e009420c83bddf50a2eb3099c3d580a2fb30"
---

## Question Answered

What does `data.model.consegna` represent in ClinicOS?

## Canonical Definition

data.model.consegna is the canonical data-model named Consegna.

## Inputs

- `id: String` (id, required, default=cuid())
- `pazienteId: String` (required, default="")
- `pazienteNome: String` (required)
- `priorita: String` (required, default="normale")
- `stato: String` (required, default="aperta")
- `tipo: String` (required, default="Monitoraggio")
- `note: String` (required)
- `scadenza: String` (required)
- `oraScadenza: String?` (nullable)
- `operatoreAssegnato: String` (required)
- `creatoDA: String` (required)
- `createdAt: DateTime` (required, default=now())
- `updatedAt: DateTime` (required)

## Outputs

Persisted PostgreSQL row for `Consegna`.

## Dependencies

None observed

## Side Effects

Database reads and writes through Prisma clients.

## Consumers

Backend routes, services, migrations, and operational jobs.

## Invariants

- `id`: identifier; required
- `pazienteId`: required
- `pazienteNome`: required
- `priorita`: required
- `stato`: required
- `tipo`: required
- `note`: required
- `scadenza`: required
- `operatoreAssegnato`: required
- `creatoDA`: required
- `createdAt`: required
- `updatedAt`: required
- index on `stato`
- index on `priorita`

## Failure Modes

Constraint violations, relation violations, unavailable database, or Prisma operation errors.

## Evidence

- `prisma/schema.prisma:377-394` — Consegna

## Related Knowledge

- `belongs-to` → `project.prisma`
