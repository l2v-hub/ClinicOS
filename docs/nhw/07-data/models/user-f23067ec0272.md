---
id: 'data.model.user'
kind: 'data-model'
title: 'User'
status: 'observed'
summary: 'Prisma persistence model User.'
bounded_contexts:
  - 'context.identity-access'
sources:
  - path: 'prisma/schema.prisma'
    symbol: 'User'
    line_start: '21'
    line_end: '36'
    confidence: 'observed'
relations:
  - type: 'belongs-to'
    target: 'project.prisma'
    evidence: 'prisma/schema.prisma'
    confidence: 'observed'
  - type: 'depends-on'
    target: 'data.model.appointment'
    evidence: 'prisma/schema.prisma'
    confidence: 'observed'
  - type: 'depends-on'
    target: 'data.model.operator'
    evidence: 'prisma/schema.prisma'
    confidence: 'observed'
tags:
  - 'prisma'
  - 'database-model'
last_verified:
  commit: 'working-tree'
  inventory_hash: '88bfed0fcf4eeef0bf5613885ddf3f844a8a14eace6d862aaf9b2a925afb484c'
---

## Question Answered

What does `data.model.user` represent in ClinicOS?

## Canonical Definition

data.model.user is the canonical data-model named User.

## Inputs

- `id: String` (id, required, default=cuid())
- `email: String` (unique, required)
- `passwordHash: String` (required)
- `fullName: String` (required)
- `role: Role` (required, default=OPERATOR)
- `entraObjectId: String?` (unique, nullable)
- `isActive: Boolean` (required, default=true)
- `lastLoginAt: DateTime?` (nullable)
- `createdAt: DateTime` (required, default=now())
- `updatedAt: DateTime` (required)
- `operator: Operator?` (nullable)
- `appointmentsCreated: Appointment[]` (required, list)

## Outputs

Persisted PostgreSQL row for `User`.

## Dependencies

- - `operator` → `Operator` (optional-one; onDelete=unspecified)
- - `appointmentsCreated` → `Appointment` (many; onDelete=unspecified)

## Side Effects

Database reads and writes through Prisma clients.

## Consumers

Backend routes, services, migrations, and operational jobs.

## Invariants

- `id`: identifier; required
- `email`: unique; required
- `passwordHash`: required
- `fullName`: required
- `role`: required
- `entraObjectId`: unique; nullable
- `isActive`: required
- `createdAt`: required
- `updatedAt`: required
- `appointmentsCreated`: required

## Failure Modes

Constraint violations, relation violations, unavailable database, or Prisma operation errors.

## Evidence

- `prisma/schema.prisma:21-36` — User

## Related Knowledge

- `belongs-to` → `project.prisma`
- `depends-on` → `data.model.appointment`
- `depends-on` → `data.model.operator`
