---
id: "data.model.room"
kind: "data-model"
title: "Room"
status: "observed"
summary: "Prisma persistence model Room."
bounded_contexts:
  - "context.facility-occupancy"
sources:
  - path: "prisma/schema.prisma"
    symbol: "Room"
    line_start: "330"
    line_end: "341"
    confidence: "observed"
relations:
  - type: "belongs-to"
    target: "project.prisma"
    evidence: "prisma/schema.prisma"
    confidence: "observed"
  - type: "depends-on"
    target: "data.model.bed"
    evidence: "prisma/schema.prisma"
    confidence: "observed"
tags:
  - "prisma"
  - "database-model"
last_verified:
  commit: "working-tree"
  inventory_hash: "72e609c2ac06a4ade4df3d8719628805e2d1606ce4aec0e6f662451324869f8e"
---

## Question Answered

What does `data.model.room` represent in ClinicOS?

## Canonical Definition

data.model.room is the canonical data-model named Room.

## Inputs

- `id: String` (id, required, default=cuid())
- `numero: String` (unique, required)
- `tipo: RoomType` (required, default=singola)
- `piano: String` (required, default="")
- `reparto: String` (required, default="")
- `stato: RoomStatus` (required, default=attiva)
- `note: String` (required, default="")
- `createdAt: DateTime` (required, default=now())
- `updatedAt: DateTime` (required)
- `beds: Bed[]` (required, list)

## Outputs

Persisted PostgreSQL row for `Room`.

## Dependencies

- - `beds` → `Bed` (many; onDelete=unspecified)

## Side Effects

Database reads and writes through Prisma clients.

## Consumers

Backend routes, services, migrations, and operational jobs.

## Invariants

- `id`: identifier; required
- `numero`: unique; required
- `tipo`: required
- `piano`: required
- `reparto`: required
- `stato`: required
- `note`: required
- `createdAt`: required
- `updatedAt`: required
- `beds`: required

## Failure Modes

Constraint violations, relation violations, unavailable database, or Prisma operation errors.

## Evidence

- `prisma/schema.prisma:330-341` — Room

## Related Knowledge

- `belongs-to` → `project.prisma`
- `depends-on` → `data.model.bed`
