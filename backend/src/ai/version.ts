// Versioning for the ClinicOS extraction schema + prompt.
// Bump these when the asset files under backend/ai-assets change in a way that
// affects extraction output, so results stay traceable to the asset version used.
export const EXTRACTION_SCHEMA_VERSION = '1.0.0';
export const EXTRACTION_PROMPT_VERSION = '1.0.0';
