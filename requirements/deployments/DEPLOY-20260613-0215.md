# DEPLOY-20260613-0215

## Included requirements

- REQ-003 — Uniformare spazio sinistro e allineamento navigazione livello 2 e 3 — GitHub Issue #5 — commit 3e10a4e

## Branch

015-unified-topnav

## Commits

- 3e10a4e REQ-003: align Scheda Paziente left gutter with shared page axis

## Areas changed

- frontend
  - frontend/src/app-additions.css (`.page-content:has(.patient-record-view)` no longer zeroes horizontal padding; inherits shared responsive gutter)
- backend — none
- prisma — none
- requirements — evidence/REQ-003/* (acceptance matrix, visual verification, before/after screenshots, data smoke), this manifest
- tooling — .claude/skills/run-clinicos/{measure,measure-detail,overflow}.mjs (Playwright gutter/overflow measurement)

## Root cause

REQ-003 was previously marked deployed, but the Scheda Paziente page still violated it:
`.page-content:has(.patient-record-view) { padding: 0 }` zeroed the gutter only on patient
detail, leaving L2 nav, L3 nav and the patient header flush against the sidebar (rect.left=96)
while all other pages used the 32px gutter (rect.left=128).

## Tests executed

- Playwright `rect.left` measurement, all 5 main pages (Dashboard, Pazienti, Scheda Paziente, Parametri, Agenda):
  BEFORE — detail @96 (flush) vs others @128. AFTER — all @128 desktop / 24px gutter tablet. PASS
- Horizontal overflow check (scrollWidth vs clientWidth), 5 pages × {desktop 1366, tablet 1024}: no overflow. PASS
- Data smoke before/after: /patients = 10 rows both. No regression. PASS
- Screenshots desktop (1366×768) + tablet (1024×768) for Scheda Paziente. PASS
- npm run build:frontend (tsc -b && vite build) — EXIT=0 PASS

## Build result

- npm run build: PASS (EXIT=0)

## Deployment

- frontend deployment: triggered
- backend deployment: not applicable (no backend changes)
- deploy provider: Vercel
- deploy project: clinicos__
- deploy URL: https://clinicos-973jy2d9t-lucalavia-2482s-projects.vercel.app
- alias: https://clinicos-eosin.vercel.app
- readyState: READY
- target: production

## Notes

- Surgical CSS-only change; sidebar, L2 and L3 behaviour untouched (only the page gutter changed).
- Vertical viewport-fill behaviour of the detail page preserved (height calc + overflow hidden);
  only left/right padding now inherits the shared `.page-content` gutter.
- Branch 015-unified-topnav does not auto-deploy frontend; production triggered manually via `vercel deploy --prod`.
