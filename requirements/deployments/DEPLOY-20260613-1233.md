# DEPLOY-20260613-1233

## Included requirements

- REQ-013 — Configurare provider AI, modello e segreti senza hardcoding — GitHub Issue #12 — commit 37ceba9
- REQ-014 — Backend upload multiplo e job di estrazione documentale — GitHub Issue #13 — commit d7c699e

## Branch

req/ai-import-foundation

## Commits

- 37ceba9 REQ-013: configure AI provider model and secrets
- 8619769 REQ-013: add deployment manifest DEPLOY-20260613-1233
- d7c699e REQ-014: add multi-file document import jobs

## Areas changed

- frontend
  - frontend/src/components/shared/AIImportStatus.tsx (new — service available/not-available badge, fetches /ai/extraction/status, no key)
  - frontend/src/components/operator/PatientList.tsx (mounts AIImportStatus next to "Nuovo paziente")
  - frontend/src/app-additions.css (badge/dot styles)
- backend
  - backend/src/ai/* (new — config, types, redact, provider-factory, version, providers/google-gemma, providers/mock)
  - backend/src/routes/ai-extraction.ts (new — GET /ai/extraction/status + /capabilities, secret-free)
  - backend/src/app.ts (mount /ai/extraction)
  - backend/src/server.ts (secret-free startup AI validation log)
  - backend/.env.example (AI_* vars, no secrets)
  - backend/package.json (@google/genai dep, test script), backend/tsconfig.json (exclude *.test.ts)
  - backend/ai-assets/clinicos-extraction.it.json, extraction-prompt.it.txt (versioned schema + prompt)
- prisma (REQ-014)
  - prisma/schema.prisma: ImportJob + ImportDocument models
  - prisma/migrations/20260613103741_add_import_jobs/ — applied to LOCAL/dev DB ONLY; NOT merged to main, so Railway prod DB is untouched (per user decision)
- backend (REQ-014)
  - backend/src/ai/upload/* (mime-sniff, validation, storage, job-service)
  - backend/src/routes/ai-jobs.ts (multi-file upload + lifecycle endpoints)
  - backend/src/ai/config.ts (AI_UPLOAD_DIR, AI_JOB_RETENTION_MIN), backend/src/app.ts (mount), backend/src/server.ts (retention sweep)
  - backend/scripts/jobs-smoke.mts (HTTP+DB integration smoke)
- frontend (REQ-014)
  - frontend/src/components/shared/DischargeImportModal.tsx (multi-file/photo upload UI)
  - frontend/src/components/shared/AIImportStatus.tsx (opens modal), app-additions.css
- documentation — none
- requirements — this manifest
- evidence — .claude/screenshots/req013-badge-available.png, req013-pazienti-badge.png, req014-import-modal.png

## Tests executed

- backend `npm test` (node:test, 9 cases): PASS
  - valid config available; missing GEMINI_API_KEY → controlled error (no throw)
  - changing AI_MODEL changes model; mock provider selectable without key
  - factory throws controlled error when google selected but unavailable
  - schema/prompt assets load + versioned; publicStatus never leaks key
  - redaction masks secrets / truncates content; env limits parse with fallbacks
- Functional endpoint smoke (GET /ai/extraction/status):
  - no key → `available:false`, errors=["GEMINI_API_KEY mancante…"], startup warns, no crash
  - key + AI_MODEL=gemini-2.0-flash → `available:true`, model swapped, **no frontend change**, log shows model only (no secret)
- Secret scan: frontend/dist bundle CLEAN; no VITE_ AI key; no real AIza* key in tracked source

## Tests executed (REQ-014)

- backend `npm test` (node:test, 20 cases total incl. 11 upload): PASS
  - magic-byte MIME sniff (pdf/png/jpg/webp/zip-docx/exe/txt)
  - EXE renamed `.pdf` rejected; forged ext (pdf bytes as `.png`) rejected; bare `.zip` rejected; DOCX accepted only with doc/docx ext
  - oversize/empty rejected; identical bytes → identical sha256 (dedup); filename path-traversal sanitized
- HTTP+DB integration smoke (`backend/scripts/jobs-smoke.mts` vs local Postgres): **ALL PASS**
  - 4 files (pdf+jpg+dup-pdf+forged-exe) → 2 accepted, 1 duplicate, 1 rejected; invalids did not drop valids
  - status exposes no storagePath; remove 1 doc recomputes total; add-more on existing job; cancel → terminal + bytes cleared
- Screenshots: badge available (green) + not-available (grey); import modal open (Seleziona file/Scatta foto, limits line)

## Build result

- npm run build (backend): PASS (exit 0) — both REQ-013 and REQ-014
- npm run build (frontend): PASS (exit 0) — both REQ-013 and REQ-014

## Deployment

- frontend deployment: not triggered — branch `req/ai-import-foundation` (Vercel prod deploys only on push to `main` per .github/workflows/deploy.yml). Preview/prod requires merge to main.
- backend deployment: not triggered — Railway prod deploys from main; this branch carries no DB migration (REQ-013 is config-only).
- deploy provider: Vercel (frontend) / Railway (backend)
- deploy URL: n/a until merge to main

## Acceptance criteria (REQ-013)

- [x] Provider e modello configurabili via environment
- [x] Chiave usata solo dal backend
- [x] Bundle frontend non contiene la chiave
- [x] Cambiando AI_MODEL cambia il modello senza modifica al frontend
- [x] Configurazione mancante produce errore controllato
- [x] Log privi di segreti e contenuto completo dei documenti

## Notes

- No GEMINI_API_KEY is committed anywhere. Key to be set in Railway secret manager (per user decision); frontend never receives it.
- Visual desktop/tablet screenshot of the import flow is deferred to the REQ-014 sandbox end-to-end run (upload modal entry point); REQ-013 behavior is proven functionally via the status endpoint above.
- No Prisma/schema/API-contract changes. Mock provider keeps CI deterministic without a real key (REQ-020).
