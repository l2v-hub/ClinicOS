# Deploy Manifest — EPIC #120 (Unificazione ingresso paziente)

Convergenza ingresso manuale + documentale su un unico `PatientIntakeDraft`, una workspace a 6 step e una conferma transazionale.

## REQ / BUG inclusi

| Issue | Fase | Titolo                                                                           | Commit (main)     |
| ----- | ---- | -------------------------------------------------------------------------------- | ----------------- |
| #121  | F1   | BUG-069 parità campi scheda/ingresso (gap analysis)                              | e2435b0           |
| #123  | F2   | BUG-071 componenti clinici condivisi                                             | 438086d           |
| #125  | F3   | BUG-073 persistenza transazionale + PatientIntakeDraft (Prisma + migrazione)     | (feat/125 merged) |
| #122  | F4   | BUG-070 workspace 6-step                                                         | 0eb3d06           |
| #124  | F5   | BUG-072 import documentale → stessa bozza + badge provenienza + guardie cliniche | 66f7262 / 01113fb |

## Deploy

- **Frontend**: Vercel `clinicos__` (alias `clinicos-eosin.vercel.app`), bundle `index-C4I6taTB.js` (build tsc autoritativo OK).
- **Backend**: Railway `clinicos-backend-production-df88` (git-connected, auto-deploy su push main); `/health = ok`.
- **Migrazioni Prisma applicate** (via `migrate deploy`): `20260628162637_add_patient_intake_draft`, `20260629040947_unique_intake_draft_import_job`.

## Verifica

- Test backend 175/175; tsc backend + frontend 0 errori.
- Route operator-gated live: `POST /intake/drafts`, `PATCH /intake/drafts/:id`, `POST /intake/drafts/:id/confirm`, `POST /intake/drafts/from-import`.
- Sicurezza clinica: blocchi allergie-contrastanti (REQ-026) + perdita-sezioni (BUG-051) applicati anche su `confirmDraft`; seed allergie solo se present/conflicting; seed idempotente per job.

## Evidenza

`requirements/evidence/BUG-070/`, `requirements/evidence/BUG-072/`.
