# DEPLOY-20260613-1600

Consolidated manifest for the AI discharge-letter import feature (REQ-013 → REQ-020).

## Included requirements

- REQ-013 — Provider AI, modello e segreti senza hardcoding — Issue #12 — commit 37ceba9 — CLOSED
- REQ-014 — Backend upload multiplo e job di estrazione — Issue #13 — commit d7c699e — open (logical-doc grouping/retake UI residual)
- REQ-015 — Integrazione Gemma + output strutturato — Issue #14 — commit d089498 — open (real-provider pending key)
- REQ-016 — Merge multi-documento con provenienza e conflitti — Issue #15 — commit d735fee — open (UI via 017)
- REQ-017 — Revisione e precompilazione form Nuovo Paziente — Issue #16 — commit ccacb22 — open
- REQ-018 — Persistenza transazionale e idempotente — Issue #17 — commit 55e5831 — open
- REQ-019 — Sicurezza, privacy, audit, retention — Issue #18 — commit 9a3c816 — open
- REQ-020 — Test end-to-end, sandbox, screenshot, gate — Issue #19 — commit 1b34091 — open

## Branch / merges to main

- ba1a0a9 (REQ-013 + REQ-014), 79228ce (REQ-015), dbb9dfa (REQ-016), 028270a (REQ-017),
  17b90d7 (REQ-018), 4b8bcf3 (REQ-019), b05f94e (REQ-020)
- Feature branches: req/ai-import-foundation, req/REQ-015-gemma-extraction, req/REQ-016-merge,
  req/REQ-017-review-form, req/REQ-018-persist, req/REQ-019-security, req/REQ-020-e2e-gate
- Deploy tags: deploy-20260613-1233, -1438, -1537

## Areas changed

- backend: `src/ai/*` (config, types, redact, provider-factory, version, providers/google-gemma,
  providers/mock, extraction-validate, merge, auth, rate-limit, audit, upload/*), routes ai-extraction + ai-jobs,
  app.ts, server.ts, ai-assets (schema/prompt/output-schema), scripts smokes
- frontend: AIImportStatus, DischargeImportModal, ImportReview, PatientList, App.tsx, app-additions.css
- prisma: ImportJob, ImportDocument, ImportAudit + 2 migrations (add_import_jobs, add_import_confirm_audit)
- ci: .github/workflows/ai-import-e2e.yml; e2e/*
- docs: requirements/PRIVACY-AI-IMPORT.md

## Tests executed

- backend `npm test` (node:test): 41/41 PASS (config/upload/extraction/merge/security)
- Integration smokes (local Postgres): jobs-smoke, confirm-smoke — PASS (with operator auth headers)
- E2E: API import-e2e (mock) PASS; Playwright happy path created a synthetic patient end-to-end at
  1366x768 + 1024x768 (0 console errors)
- CI workflow AI Import E2E Gate run 27469101090: secret-scan PASS, gate PASS, real-provider PASS
  (self-skipped), browser-e2e best-effort (non-blocking)
- Secret scan: CLEAN (repo + frontend bundle, no AIza key, no VITE_ key)

## Build result

- npm run build (backend): PASS
- npm run build (frontend): PASS

## Deployment

- frontend (Vercel): SUCCESS across the chain — https://clinicos-eosin.vercel.app HTTP 200
- backend (Railway): SUCCESS — both prisma migrations applied to prod; `/health` 200; `/patients`
  regression PASS (no data loss); `/ai/extraction/status` live; import endpoints enforce operator
  auth in prod (401 without header, 201 with)
- prod backend: https://clinicos-backend-production-df88.up.railway.app

## Notes

- Secrets: GEMINI_API_KEY is NOT committed; set it in the Railway secret manager to enable real
  extraction (REQ-015) and the gated real-provider CI job. Until then the prod service reports
  available:false (controlled) and the mock path covers CI.
- Auth is a pragmatic role-gate (no IdP in the app); replace with verified tokens when an IdP exists.
- Residual UI: REQ-014 multi-photo-as-one-logical-document grouping + one-click retake.

## Action required (user)

Set `GEMINI_API_KEY` in Railway → unlocks real extraction, enables closing #14, and the
real-provider CI job. Then the remaining open issues can be verified end-to-end with real (synthetic)
documents and closed.
