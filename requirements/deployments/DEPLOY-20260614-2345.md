# DEPLOY-20260614-2345

AI Runtime service deployment and backend rewiring (REQ-023/024).

## Included requirements

- REQ-023 — AI Runtime service (FastAPI/Agno, clinicos-ai-runtime) — Issue #22 — commits c773f57, 191ca74, 7005553 — CLOSED
- REQ-024 — Backend rewiring: job-service.ts uses HTTP client to AI runtime, no Google/provider SDK — Issue #23 — commits c773f57, 191ca74, 7005553 — CLOSED

## Services deployed

- **clinicos-ai-runtime** (Railway, new service): https://clinicos-ai-runtime-production.up.railway.app — healthcheck /v1/runtime/health PASS
- **clinicos-backend** (Railway): env vars AI_RUNTIME_URL + AI_RUNTIME_SERVICE_TOKEN applied — build PASS, healthcheck PASS

## Commits to main

- c773f57 — REQ-023/024: wire Node backend to AI runtime
- 191ca74 — REQ-023/024: fix storeFile args and export removeDocument
- 7005553 — REQ-023/024: fix sweepExpiredDirs typo in import

## Areas changed

- backend: `src/ai/upload/job-service.ts` — replaced @google/genai calls with neutral HTTP client (POST /v1/document-jobs → POST :id/run → poll GET :id → GET :id/result)
- infra: Railway service `clinicos-ai-runtime` created (Python FastAPI/Agno, Dockerfile, root dir `/clinicos-ai-runtime`)
- env: backend receives `AI_RUNTIME_URL` and `AI_RUNTIME_SERVICE_TOKEN`; no Google/provider SDK in backend

## Build result

- npm run build:backend (Railway): PASS
- healthcheck /health: PASS (HTTP 200)
- clinicos-ai-runtime healthcheck /v1/runtime/health: PASS (Online)

## Deployment

- clinicos-ai-runtime (Railway): Online — https://clinicos-ai-runtime-production.up.railway.app
- clinicos-backend (Railway): Online — https://clinicos-backend-production-df88.up.railway.app

## Notes

- AI runtime built from `clinicos-ai-runtime/` folder, Dockerfile build, root dir `/clinicos-ai-runtime`
- Token shared between services via Railway env vars (not in repo)
- Backend has zero provider SDK references; all AI calls go through AI_RUNTIME_URL
