# DEPLOY-20260616-0829

## Included requirements

- REQ-030 — Visualizzare Revisione e Scheda Paziente come sezioni testuali — GitHub Issue #34
- (REQ-031 / #35 — duplicate of #34, closed as duplicate)

## Summary

Frontend: shared `NarrativeClinicalSection` renders each clinical section as one faithful,
editable text block with semantic bold tags (no HTML, no table). Added a "Sezioni Cliniche
(testo)" tab to the Scheda Paziente (PatientDetail) consuming the REQ-029 narrative-sections
API; empty sections show "Aggiungi informazioni"; edits save to reviewedText (original never
overwritten). The import review (REQ-027) already presents the same narrative blocks. Legacy
structured tabs coexist as secondary areas and are never auto-populated by the import.

## Changes (frontend)

- `frontend/src/components/shared/sections/NarrativeClinicalSection.tsx`.
- `frontend/src/components/operator/cartella/NarrativeSectionsTab.tsx` (fetch REQ-029 API, edit/save).
- `PatientDetail.tsx`: new 'sezioni-narrative' tab in the Clinica group + render.
- `app-additions.css`: narrative-section styles.
- `e2e/req030-patient-narrative-shots.mjs` + screenshots.

## Tests / verification

- `tsc -b` exit 0 + `vite build` ✓.
- screenshots desktop 1366 + tablet 1024 (Scheda Paziente narrative sections, empty, edited).
- data-smoke /patients 200, 19.

## Deployment

- frontend: Vercel via deploy.yml (auto-deploy on push to main).
