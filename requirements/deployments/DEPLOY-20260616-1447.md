# DEPLOY-20260616-1447

## Included requirements

- REQ-035 — Popolare realmente i blocchi narrativi con il testo estratto (BLOCKER) — GitHub Issue #43

## Summary

Fixes empty narrative cards: the extracted section text (in the OCR markdown rawText) is now
parsed into the per-section narrative fields, so cards show the content and Modifica opens a
prefilled editor. No new AI call.

## Changes

- backend new `markdown-parse.ts` (parseMarkdownSections / parseNarrativeFromMarkdown / detectSectionLoss).
- backend `runJob`: `_narrative` from rawText markdown (fallback to sections pass / rawText).
- backend `getJobResult` + idempotent `rebuildNarrativeDraftFromExistingExtraction` self-heal old jobs (no AI).
- frontend `DischargeImportModal`: effectiveSections prefers the source carrying section text.
- tests: `markdown-parse.test.ts` (11).

## Tests / verification

- backend 96/96 · frontend 14/14 · `tsc` x2 + `vite build` ✓.
- E2E: anamnesis editor prefilled with "## Anamnesi Patologica Recente:" + Remota subtitle. /patients 200, 19.

## Deployment

- backend: Railway (deploy-backend.yml). frontend: Vercel (deploy.yml).
