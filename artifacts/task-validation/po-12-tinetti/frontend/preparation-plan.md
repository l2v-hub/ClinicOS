# PO12 frontend preparation

Root authorized preparation only at baseline `470a5fe7a7eee4a9b0a0b219947432049b9de977`. No application edits precede the root GO after PO11 live verification. The existing hierarchy supplies integration, backend execution and independent review; this worker is the sole writer in the isolated UI worktree.

The normative model is the current `ScalaTinettiTab.tsx`, source SHA256 `7785059cceedc3ff85f051a60ff1cbdaa48145f03fb7f93ab7d53696ca6c8b09`. Preserve all 20 item IDs, ordering, 48 options, descriptions and thresholds: 10 balance items maximum 16, 10 gait items maximum 12, total maximum 28. The attached PDF SHA256 `feca88c71bc7b6c9b53a812979f222e0769d688380673995277e8490db8c3ff6` is reference material; the existing four DX/SX step-length/height answers are retained. No new clinical validation or 26-point variant is claimed.

Backend and frontend agreed on the DTO before application editing. Type `tinetti`, version `tinetti-it-2026-09-22-v1`; flat answers with the 20 Italian item keys and `notes`, all clinical answers null or a permitted integer. Notes preserve whitespace/newlines, maximum 4000 codepoints. History/DTO expose `answeredCount` 0–20 and `completion:{complete,missingPaths}`. Partial `result` is null; complete result is `{balance,gait,total,riskBand:'high'|'moderate'|'low',label}`. Zero counts as answered. The API keeps the PO11 envelopes for `current?type=tinetti` and typed history; 422 `missingPaths` are top-level item names. No Tinetti attestations.

Snapshot uses common identity/author/date/predecessor fields and `items:[{id,label,score,description,group:'balance'|'gait'}]` in source order, plus complete `result`, `notes` and `provenance`. `form` carries type/version and both source/reference SHA256 values. Exact description/provenance strings will be matched to the backend contract, not inferred from serialized object order.

After GO, extract production definition/validation/scoring and add the Tinetti branch to shared DTO/client/store/workspace/summary/history/archive code. Keep new form in two compact groups with progress out of 20 (notes excluded), empty clinical selections, accessible radios, visible patient identity, preserved date/DST correction semantics, and existing replay/CAS/final/PDF behavior. New source files remain below 500 lines; shared manifests and lockfiles remain integration-owned.

The same screen will show read-only legacy history with detail and print. Remove legacy creation/edit/delete and any tab used as a filter. A pure legacy adapter retains record ID/date/createdAt/operator text/notes/raw answers and never mutates Cartella JSON or invents authenticated authors, finalization, model versions or exact clinical instants. Complete valid records retain prior mathematical results; missing/-1/out-of-domain answers produce no result/risk and an explicit incomplete/invalid indication. Legacy IDs never become versioned predecessor IDs. General print counts will explicitly identify historical records. Backend owns locked preservation of the legacy JSON branch during PUT Cartella.

Focused validation will import production definition/scoring: 48 choices, min/max, bands at 18/19/23/24, 20 missing cases, invalid values, zero versus null, notes and legacy invariance. Exercise two group combination spaces if inexpensive (11664 balance, 2304 gait), type/patient isolation, retries/CAS, snapshot mismatch rejection, archive links and PAINAD/Transfers regressions. Run direct Node test runner and TypeScript/build, baseline-relative lint, source/bundle secret scan, source manifest and compatible artifact manifest. Root owns browser synthetic end-to-end checks, real PostgreSQL/HTTP integration, PDF render inspection and release.

Ruflo guidance was advisory and reachable. Memory recall returned no directly relevant PO10/PO11 assessment entry; the tracked contract, source and accepted prior implementation are authoritative. No tracked repository collaboration harness or AGENTS.md was found in this worktree; user-provided repository instructions apply. No performance improvement is claimed without measurement.
