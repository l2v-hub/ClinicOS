import { readFileSync, writeFileSync } from 'node:fs';
const definition = JSON.parse(readFileSync('C:/Workspace/ClinicOSHouse-worktrees/po12-tinetti-backend/artifacts/task-validation/po-12-tinetti/backend/definition-snapshot.json', 'utf8'));
const items = definition.groups.flatMap(group => group.items.map(item => ({ id: item.id, label: item.label, group: group.id, options: item.options.map(option => option.description) })));
writeFileSync('frontend/src/lib/assessments/tinettiItems.ts', `import type { TinettiItemId } from './tinettiTypes';\nexport interface TinettiItem { id: TinettiItemId; label: string; group: 'balance' | 'gait'; options: readonly string[] }\nexport const TINETTI_ITEMS: readonly TinettiItem[] = ${JSON.stringify(items, null, 2)};\nexport const TINETTI_PROVENANCE = ${JSON.stringify(definition.provenance)};\n`);
const maximum = Object.fromEntries(definition.groups.flatMap(group => group.items.map(item => [item.id, item.options.length - 1])));
writeFileSync('frontend/src/lib/assessments/tinettiTypes.ts', `export const TINETTI_VERSION = 'tinetti-it-2026-09-22-v1' as const;\nexport const TINETTI_SOURCE_SHA256 = ${JSON.stringify(definition.source.sha256)};\nexport const TINETTI_REFERENCE_SHA256 = ${JSON.stringify(definition.referenceSha256)};\nexport const TINETTI_MAXIMUM = ${JSON.stringify(maximum, null, 2)} as const;\nexport type TinettiItemId = keyof typeof TINETTI_MAXIMUM;\nexport type TinettiScore = 0 | 1 | 2;\nexport type TinettiAnswers = { [K in TinettiItemId]: (typeof TINETTI_MAXIMUM)[K] extends 1 ? 0 | 1 | null : TinettiScore | null } & { notes: string };\nexport interface TinettiResult { balance: number; gait: number; total: number; riskBand: 'high' | 'moderate' | 'low'; label: string }\nexport interface TinettiSnapshotItem { id: TinettiItemId; label: string; score: TinettiScore; description: string; group: 'balance' | 'gait' }\n`);
