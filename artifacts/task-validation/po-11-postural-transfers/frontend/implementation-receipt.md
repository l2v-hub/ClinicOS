# PO11 frontend scope and integration handoff

Root GO authorizes the isolated frontend implementation at baseline `d659c1b459eb72a2920cbd9f678a2c11f9bf7efd`, after PO10 Railway deployment `0fde62f8` succeeded with migration/health200 and Vercel `dpl_DMC6zMSbrq2GgN6FiUhgUUVnXQnN` reached READY with alias/source SHA verified. The root hierarchy supplies coordination, backend work, independent review and browser/PDF validation. The frontend worker has one writing checkout and no publication authority.

The final implementation uses a discriminated PAINAD/Transfers DTO and a session-owned draft map filtered by patient and type. Nested answers are deep-copied and request payloads deeply frozen; schema-aware equality ignores object-key ordering while preserving exact text. Existing UUID replay, CAS, explicit conflict resolution, final snapshot immutability and patient/session callback fencing remain. Transfers completeness comes from the saved server response; incomplete preview and 422 paths identify fields. Corrections retain the predecessor's clinical instant, including DST occurrence and precision.

Transfers has five readable groups, no clinical preselection, nine owned and three plain aids, separate transfer choices (8/8/6), explicit unavailable context and no numerical result. Short choices wrap at content width with minimum44px height; transfer modes use responsive min220px columns; aids use two columns above600px and one below. Root PAINAD sticky identity/tablet CSS remains byte-identical to the baseline.

The workspace requests the server-resolved current final and preserves access to historical records. Personal attestations are separate, snapshot-bound events; capabilities/registered qualifications come from the backend, receipts bind actor/assessment/hash/kind, response-loss retries preserve exact bodies, and counts/history do not modify the original PDF. Archive metadata validates the type/version pairing; folder branches and reciprocal navigation carry explicit assessment IDs and types.

Integration API: `PatientDetail initialTab="postural_transfers"`. `AssessmentWorkspace` accepts `type`, `initialAssessment:{id,type}`, the same App-owned `draftStore`, and `onOpenArchive(documentId,{id,type})`. Legacy `initialAssessmentId` and default PAINAD type remain supported. `DocumentiTab onOpenAssessment({id,type})` routes to the matching module and optional expectedAssessmentType fences archive focus.

The previous validation used the direct Node runner: 20 Transfers tests plus 72 PAINAD/archive/navigation/identity regressions passed against source state `96062fccbd129c4256330d72f77c136ec6571cc71e323611dafd046f18eee0fa` (runtime `6aa41542eb58df6aea71d4555c3d28678525fa3f06e671e4bad933366ef0ea61`). Root integrated those 34 files as `3d8570`. Exact copies of the previous manifest, receipt, 92-test log, build log, lint and scan reports are retained with the `pre-date-fix-` prefix; their hashes are checked against the preserved artifact manifest.

The reviewer-requested admission-date refinement changes exactly three source files. Rendering permits a temporarily invalid raw admission date in the local dirty draft and displays an adjacent accessible error. Save continues to validate that same raw value and cannot silently save the previous date. The shared calendar helper accepts real civil dates in years 0001–9999, rejecting 0000 and five-digit years. Correction to 1999 removes the error and produces the corrected save payload. All other answers and unsaved-exit state survive validation failure.

The refinement runs 17 focused definition, draft-store and SSR/UI tests, including the new invalid-date retention/correction regression and all 384 transfer combinations. It does not claim a rerun of the prior 92-test suite. The current build, baseline-relative lint and source+bundle scan bind to the updated source manifest; the exact three-file old/new hash delta is in `date-fix-delta.json`. Root owns browser geometry, keyboard behavior, real PostgreSQL/HTTP actions, PDF rendering and release. The known-signature scan is a limited check, and no performance improvement is claimed.
