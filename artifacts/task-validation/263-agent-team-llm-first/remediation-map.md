# Remediation Map — issue #263, attempt 2

Codex qa_result attempt 1 (comment 4952257860, subject `69564a85dd9f9dbd0852dad37275722329fc91bd`,
preserved verbatim at `agent-team/qa-result.json` and `codex-qa-report.md`) → attempt-2 changes.
Every finding maps to changed code, changed evidence, or an explicit unresolved result.

| Finding | Severity | Resolution | Code | TDD evidence |
|---|---|---|---|---|
| QA-263-001 | critical | RESOLVED — runtime rebuilds attempt/open-findings/coordinates from GitHub protocol comments and hands them to Claude; same branch/worktree/PR reused; integration test drives the real reconciler path qa-failed → ready-for-qa | `src/core/history.mjs` (new), `src/runtime.mjs` | tdd/r1-history-red.txt → green; tdd/r7-loop-red.txt → green (`remediation-loop.test.mjs`) |
| QA-263-002 | critical | RESOLVED — scoped `allowedTools` config (validated, no wildcards/bypass), separate `developmentTimeoutMs`/`qaTimeoutMs`, empty-policy launch refusal with precise diagnostic, structured-output schema validation before any GitHub change; worker options live-verified by doctor against installed `claude --help` | `config/*`, `src/core/config.mjs`, `src/workers/*.mjs`, `src/commands/doctor.mjs` | tdd/r2-operability-red.txt → green; tdd/r4b-schema-intake-red.txt → green; checks/doctor-smoke.json (`claude-worker-options: ok`) |
| QA-263-003 | high | RESOLVED — dedicated `claim.schema.json`; acquire validates the claim and parses competitors schema-tolerantly; `refreshGitHubClaim` (comment edit, lease extension), `releaseGitHubClaim` (work.claim_released), `recoverActiveClaim`; runtime releases claims on worker failure; restart refresh wired into loop startup | `protocol/schemas/claim.schema.json`, `src/core/locks.mjs`, `src/runtime.mjs`, `src/core/recovery.mjs` | tdd/r4-claims-red.txt → green (`claim-lifecycle.test.mjs`); release path in `remediation-loop.test.mjs` test 3 |
| QA-263-004 | high | RESOLVED — doctor now checks labels existence, push permission, referenced prompt/schema files, worker permission policy, verified worker CLI options (claude + codex exec), realistic timeouts, writable runtime root, git-ignored roots (C-quoted Windows path normalization), duplicate supervisor with configured `heartbeatTimeoutMs`; 14 unit cases incl. one negative per hard failure | `src/commands/doctor.mjs`, `src/core/locks.mjs` | tdd/r5-doctor-red.txt → green; tdd/r10-checkignore-red.txt → green; live checks/doctor-smoke.json 21/21 `ok:true` |
| QA-263-005 | high | RESOLVED — non-circular `evidence_binding` envelope published post-commit binds PR head → committed manifest blob → per-artifact git blob IDs + SHA-256 of committed contents (read from the commit, unsanitized/untrimmed); disagreement between envelope and committed manifest rejected; verified against a real git repository incl. tampering | `protocol/schemas/evidence-binding.schema.json`, `src/core/binding.mjs`, `src/adapters/git.mjs`, `src/adapters/process-runner.mjs` | tdd/r6-binding-red.txt → green (`evidence-binding.test.mjs`, real git) |
| QA-263-006 | high | RESOLVED — `nextRemediationState` wired into the runtime intake path: three equivalent qa-failed results publish a schema-valid `worker.blocked` message with structured reason + qa_history and transition to `blocked` without launching Claude | `protocol/schemas/worker-blocked.schema.json`, `src/runtime.mjs` | `remediation-loop.test.mjs` test 2 (tdd/r7-loop-red.txt → green) |
| QA-263-007 | medium | RESOLVED — `status` projects GitHub protocol state (claims/leases/attempts/handoffs/QA decisions/PR head) plus local liveness; `stop` waits lease-safe until heartbeat stale with timeout; `start` acknowledges only after the detached loop owns the runtime; loop startup recovers and refreshes my active claims from GitHub | `src/core/status-projection.mjs`, `src/core/recovery.mjs`, `src/commands/{status,stop,start}.mjs` | tdd/r8-lifecycle-red.txt → green (`supervisor-lifecycle.test.mjs`) |
| QA-263-008 | medium | UNRESOLVED — EXTERNAL (owner: repository-owner). GitHub Actions jobs do not start: account payment failed / Actions spending limit must be increased. Not code-rooted; not faked, not relabeled. Fresh post-push check-run state reported in the handoff comment; original finding preserved verbatim | — | handoff `unresolved_findings` + post-push CI probe |
| QA-263-009 | low | RESOLVED — `sanitizeText` strips trailing whitespace deterministically; all committed evidence normalized (Codex artifacts untouched, 0 violations found in them); final suite re-run through a harness recording real per-command started/finished timestamps and exit codes (`checks/command-log.jsonl`); fresh `git diff --check` exit 0 | `src/core/sanitize.mjs` | tdd/r9-sanitize-red.txt → green; checks/git-diff-check.txt; checks/command-log.jsonl |

Attempt-2 claim: comment 4955564065 (lease e2431891…, refreshed for server-clock skew, expires 2026-07-13T15:37:44Z).

## Attempt 3 (remediation of qa_result comment 4957169054)

| Finding | Severity | Resolution | Code | TDD evidence |
|---|---|---|---|---|
| QA-263-010 | high | RESOLVED — `runDevelopment` publishes a schema-valid `work.claim_released` after a successful handoff is preserved (failure-path release unchanged); released leases are excluded from acquire arbitration, `recoverActiveClaim`, and the status projection even while unexpired, matched by `lease_id` so a schema-invalid claim tolerated by the projection is cleared too; the stale attempt-2 claim was released live (comment 4957297859); integration test proves ready-for-qa with `active_claim: null` and handoff-before-release ordering | `src/runtime.mjs`, `src/core/locks.mjs`, `src/core/status-projection.mjs` | tdd/a3-1-claim-release-red.txt (4 fail) → tdd/a3-1-claim-release-green.txt (16/16) |
| QA-263-011 | critical | RESOLVED — new `src/core/worker-policy.mjs` builds the exact documented argv `--print --output-format json --permission-mode acceptEdits --tools … --allowedTools … --disallowedTools …` and refuses (before any spawn) policies exposing `Task`/`Agent`, missing any of the ten nested-execution deny rules, or containing bypass/dangerous tokens; the identical validator runs in `validateConfig` and doctor (`worker-permission-policy`); doctor also requires documented `--tools`/`--disallowedTools` in the installed CLI; `default.json` ships tool surface Read/Edit/Write/Glob/Grep/Bash plus denies for Task, Agent, and claude/claude.exe/npx-claude invocation; process-runner timeout escalates to a tree kill (win32 `taskkill /T /F`) with the child pid exposed and liveness-tested; worker prompt states nested execution is unavailable | `src/core/worker-policy.mjs` (new), `src/workers/claude-development-worker.mjs`, `src/core/config.mjs`, `src/commands/doctor.mjs`, `src/adapters/process-runner.mjs`, `config/default.json`, `config/config.schema.json`, `prompts/claude-development.md` | tdd/a3-2-tool-policy-red.txt (13 fail) → tdd/a3-2-tool-policy-green.txt (42/42); full suite 80/80 |
| QA-263-008 | medium | UNRESOLVED — EXTERNAL (owner: repository-owner). GitHub Actions jobs still cannot start: account payment failed / Actions spending limit must be increased. Not code-rooted; not faked, not relabeled | — | handoff `unresolved_findings` + post-push CI probe |

Attempt-3 claim: comment 4957297633 (lease 2f1099fa-b2d5-49a6-bcac-e4e1cbd12d8e); stale attempt-2
claim released in comment 4957297859 (`claim-263-2-e2431891-released`).

## Attempt 6 (remediation of qa_result comments 4967120522 attempt 4 and 4967217638 attempt 5)

Verbatim Codex QA results preserved at `agent-team/qa-result-attempt-4.json` and
`agent-team/qa-result-attempt-5.json`.

| Finding | Severity | Resolution | Code | TDD evidence |
|---|---|---|---|---|
| QA-263-012 | medium | RESOLVED — smallest safe workflow correction: `VITE_API_URL: http://localhost:3001` added to the browser-e2e **job env** of `ai-import-e2e.yml`, so the production Vite build bakes the CI backend URL in and both deterministic desktop/tablet paths reach the local backend/mock runtime. Production fallback in `frontend/src/config.ts` untouched and pinned by regression test (Railway URL + PROD/dev selection asserted verbatim) | `.github/workflows/ai-import-e2e.yml` | tdd/a6-1-ci-binding-red.txt (1 fail) → tdd/a6-1-ci-binding-green.txt (2/2); `agent-team/tests/unit/ci-browser-e2e-config.test.mjs` |
| QA-263-013 | high | RESOLVED — recovered coordinates validated before spawn (existence + expected branch via `rev-parse --abbrev-ref HEAD` + expected repository via `remote get-url origin`); stale coordinates safely repaired: live checkout of the same branch reused, else `worktree prune` + recreation from the existing branch; no destructive git command on any recovery path (occupied dir / missing branch fail closed); `runProcess` refuses a nonexistent cwd with `working directory does not exist: <cwd>` instead of a misleading `spawn claude ENOENT`; worker failure releases the claim **and** restores `ready-for-dev` + `assigned-to-claude` while removing `agent-working`; `once` returns `ok:false` (exit 1 via existing cli mapping) whenever `development.errors`/`qa.errors` is non-empty | `agent-team/src/adapters/git.mjs`, `agent-team/src/adapters/process-runner.mjs`, `agent-team/src/runtime.mjs`, `agent-team/src/core/reconciler.mjs`, `agent-team/src/commands/once.mjs` | tdd/a6-2-worktree-recovery-red.txt (5 fail) → green (11/11); tdd/a6-3-failure-reporting-red.txt (2 fail) → green (6/6); `git-worktree-recovery.test.mjs`, `once-failure-reporting.test.mjs`, extended `process-runner.test.mjs` + `remediation-loop.test.mjs` |

Attempt-6 claim: comment 4967240019 (lease c5f9b01e-bdf5-4360-ab8b-8fff7d34cb78, refreshed during
execution). Full suite after both fixes: 90/90; doctor 21/21; build exit 0; diff-check exit 0;
static scan clean (`checks/a6-secret-prohibited-scan.txt`).

## Attempt 9 (remediation of qa_result comment 4970198387; attempt 8 interrupted, work preserved)

Attempt 8 was terminated externally mid-run. All of its uncommitted work (QA-263-014 TDD cycle
and fixes) was preserved, inspected, re-verified green, and carried into this attempt — nothing
was discarded or blindly overwritten.

| Finding | Severity | Resolution | Code | TDD evidence |
|---|---|---|---|---|
| QA-263-014 | high | RESOLVED — the browser journey now completes the real F5 (#124) handoff: review → IntakeWorkspace step 3 (explicit allergy state `paziente_nega` asserted visible + #235 therapy acceptance) → steps 4/5 → step 6 (#235 demographics acceptance, blocking checklist asserted cleared) → StepVerifica "Crea paziente" (explicit "Crea comunque" duplicate path); persistence asserted against the backend `/patients` API **and** UI after full SPA reload — never transient form text; non-persisted outcome remains a hard failure with sanitized diagnostics. Reproduced product defect fixed: the #244 allergy-status selector was dead in intake mode — `StepClinica` now wires `status`/`onStatusChange` to the draft and `IntakeWorkspace` carries `allergieStatus` into `cartella.allergieStatus` (same key PatientDetail uses). Local Playwright run infrastructure-blocked (no local install; package installation outside worker permissions) — exact-SHA browser evidence remains the CI `browser-e2e` job at the pushed PR head, journey contract pinned deterministically by unit test | `e2e/import-happy-path.mjs`, `frontend/src/components/shared/intake/StepClinica.tsx`, `frontend/src/components/shared/intake/IntakeWorkspace.tsx` | tdd/a8-1-journey-red.txt (3 fail) → tdd/a8-1-journey-green.txt (3/3); `agent-team/tests/unit/e2e-import-journey.test.mjs`; frontend build `checks/build.txt` + `checks/build-vite.txt` |
| QA-263-015 | critical | RESOLVED — (1) claim cwd authority: `isValidWorktree` additionally requires a **registered `git worktree list --porcelain` root** (nested subpaths pass branch/origin checks because git resolves upward — the attempt-8 vector, seeded by the fabricated `worktreeRoot/issue-N` default in `history.mjs` coordinates); new read-only `git.resolveIssueWorktree` runs **before** the claim, the claim carries the resolved coordinates, `runtime.mjs` verifies prepared == claimed (fail closed + claim release + label restore on divergence) and passes identical coordinates to the worker; (2) lease refresh during execution: `startLeaseHeartbeat` refreshes the claim every `leaseRefreshMs` for the whole Claude run, a failed refresh aborts via AbortSignal; (3) process ownership: `runProcess` accepts the signal (no spawn under an already-lost lease; mid-run abort kills the tree with `aborted: <reason>`), owned-children registry + `killOwnedProcessTrees()` (bounded sync taskkill /T /F + direct-kill fallback) + process exit hook, wired into the graceful `start.mjs` loop exit; a lost lease surfaces as a worker failure → claim released, `agent-working` removed, `ready-for-dev` + `assigned-to-claude` restored (deterministic GitHub state) | `agent-team/src/adapters/git.mjs`, `agent-team/src/core/locks.mjs`, `agent-team/src/adapters/process-runner.mjs`, `agent-team/src/workers/claude-development-worker.mjs`, `agent-team/src/runtime.mjs`, `agent-team/src/commands/start.mjs` | tdd/a9-1-ownership-red.txt (11 fail / 2 deliberate pins) → tdd/a9-1-ownership-green.txt (13/13); `claim-worktree-authority.test.mjs`, `lease-heartbeat.test.mjs`, `process-ownership.test.mjs`, `process-exit-ownership.test.mjs` (+ fixture), extended `git-worktree-recovery.test.mjs`, `remediation-loop.test.mjs`, `once-failure-reporting.test.mjs` |

Attempt-9 execution context: launched directly by the supervisor on the preserved worktree
(C:/tmp/ClinicOS-agent-team, the registered authoritative checkout of
`codex/agent-team-architecture`); no separate work.claim comment was supplied for this attempt.
Full suite after both fixes: 106/106 (88 unit + 18 integration); frontend `tsc -b` + `vite build`
exit 0; static scan clean (`checks/a9-secret-prohibited-scan.txt`).
