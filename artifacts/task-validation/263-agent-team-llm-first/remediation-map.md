# Remediation Map — issue #263, attempt 2

Codex qa_result attempt 1 (comment 4952257860, subject `69564a85dd9f9dbd0852dad37275722329fc91bd`,
preserved verbatim at `agent-team/qa-result.json` and `codex-qa-report.md`) → attempt-2 changes.
Every finding maps to changed code, changed evidence, or an explicit unresolved result.

| Finding | Severity | Resolution | Code | TDD evidence |
|---|---|---|---|---|
| QA-263-001 | critical | RESOLVED — runtime rebuilds attempt/open-findings/coordinates from GitHub protocol comments and hands them to Claude; same branch/worktree/PR reused; integration test drives the real reconciler path qa-failed → ready-for-qa | `src/core/history.mjs` (new), `src/runtime.mjs` | tdd/r1-history-red.txt → green; tdd/r7-loop-red.txt → green (`remediation-loop.test.mjs`) |
| QA-263-002 | critical | RESOLVED — scoped `allowedTools` config (validated, no wildcards/bypass), separate `developmentTimeoutMs`/`qaTimeoutMs`, empty-policy launch refusal with precise diagnostic, structured-output schema validation before any GitHub change; worker options live-verified by doctor against installed `claude --help` | `config/*`, `src/core/config.mjs`, `src/workers/*.mjs`, `src/commands/doctor.mjs` | tdd/r2-operability-red.txt → green; tdd/r4b-schema-intake-red.txt → green; checks/doctor-smoke.json (`claude-worker-options: ok`) |
| QA-263-003 | high | RESOLVED — dedicated `claim.schema.json`; acquire validates the claim and parses competitors schema-tolerantly; `refreshGitHubClaim` (comment edit, lease extension), `releaseGitHubClaim` (work.claim_released), `recoverActiveClaim`; runtime releases claims on worker failure; restart refresh wired into loop startup | `protocol/schemas/claim.schema.json`, `src/core/locks.mjs`, `src/runtime.mjs`, `src/core/recovery.mjs` | tdd/r4-claims-red.txt → green (`claim-lifecycle.test.mjs`); release path in `remediation-loop.test.mjs` test 3 |
| QA-263-004 | high | RESOLVED — doctor now checks labels existence, push permission, referenced prompt/schema files, worker permission policy, verified worker CLI options (claude + codex exec), realistic timeouts, writable runtime root, git-ignored roots (C-quoted Windows path normalization), duplicate supervisor with configured `heartbeatTimeoutMs`; 14 unit cases incl. one negative per hard failure | `src/commands/doctor.mjs`, `src/core/locks.mjs` | tdd/r5-doctor-red.txt → green; tdd/r10-checkignore-red.txt → green; live checks/doctor-smoke.json 21/21 `ok:true` |
| QA-263-005 | high | RESOLVED — non-circular `evidence_binding` envelope published post-commit binds PR head → committed manifest blob → per-artifact git blob IDs + SHA-256 of committed contents (read from the commit, unsanitized/untrimmed); disagreement between envelope and committed manifest rejected; verified against a real git repository incl. tampering | `protocol/schemas/evidence-binding.schema.json`, `src/core/binding.mjs`, `src/adapters/git.mjs`, `src/adapters/process-runner.mjs` | tdd/r6-binding-red.txt → green (`evidence-binding.test.mjs`, real git) |
| QA-263-006 | high | RESOLVED — `nextRemediationState` wired into the runtime intake path: three equivalent qa-failed results publish a schema-valid `worker.blocked` message with structured reason + qa_history and transition to `blocked` without launching Claude | `protocol/schemas/worker-blocked.schema.json`, `src/runtime.mjs` | `remediation-loop.test.mjs` test 2 (tdd/r7-loop-red.txt → green) |
| QA-263-007 | medium | RESOLVED — `status` projects GitHub protocol state (claims/leases/attempts/handoffs/QA decisions/PR head) plus local liveness; `stop` waits lease-safe until heartbeat stale with timeout; `start` acknowledges only after the detached loop owns the runtime; loop startup recovers and refreshes my active claims from GitHub | `src/core/status-projection.mjs`, `src/core/recovery.mjs`, `src/commands/{status,stop,start}.mjs` | tdd/r8-lifecycle-red.txt → green (`supervisor-lifecycle.test.mjs`) |
| QA-263-008 | medium | UNRESOLVED — EXTERNAL (owner: repository-owner). GitHub Actions jobs do not start: account payment failed / Actions spending limit must be increased. Not code-rooted; not faked, not relabeled. Fresh post-push check-run state reported in the handoff comment; original finding preserved verbatim | — | handoff `unresolved_findings` + post-push CI probe |
| QA-263-009 | low | RESOLVED — `sanitizeText` strips trailing whitespace deterministically; all committed evidence normalized (Codex artifacts untouched, 0 violations found in them); final suite re-run through a harness recording real per-command started/finished timestamps and exit codes (`checks/command-log.jsonl`); fresh `git diff --check` exit 0 | `src/core/sanitize.mjs` | tdd/r9-sanitize-red.txt → green; checks/git-diff-check.txt; checks/command-log.jsonl |

Attempt-2 claim: comment 4955564065 (lease e2431891…, refreshed for server-clock skew, expires 2026-07-13T15:37:44Z).
