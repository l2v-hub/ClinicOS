# PO13 MNA assigned frontend preparation

Root assigned `C:/Workspace/ClinicOSHouse-worktrees/po13-mna-ui`, branch `codex/po13-mna-ui`, baseline `4e594aa39cb2cf9ef3bfc403e4680ae66a41c3be`. This phase authorizes claim, runtime and DTO artifacts only. Application implementation requires an explicit root GO after PO12 deployment and live verification.

The worker is the sole writer in this checkout. The existing root/backend/reviewer hierarchy remains in use; no extra agents, servers or development ports are started. Ruflo guidance and claim inspection were reachable. Its claim is a coordination ledger entry and does not grant application or publication authority. The tracked agent-team README describes GitHub issue orchestration; this directly assigned worker uses the root assignment, existing hierarchy and local evidence, without posting a GitHub claim or starting its supervisor.

## Frozen contract and preparation lineage

The agreed DTO snapshot SHA256 is `9d24743fafe6b78ce71dc4c119603ccae1bdde6babbe44907b30bf79b8e9eb2a`; the root task-contract snapshot SHA256 is `8f08f0f2b141c29ec0a3717ccab8ac67790e619ca0628e8d08a198654f53d961`. There are no pending DTO decisions. The root task contract remains authoritative.

The initial UX and implementation notes are retained as immutable snapshots from the prior preparation directory. Their references to the PO12 staging checkout describe their origin. This document records the newly assigned PO13 checkout; it does not revise the clinical content or agreement. The origin artifact-manifest snapshot SHA256 is `ab71a417508d635ca9410e86ad9b2b7a48169d96a98032ba725de7d9e19a1852`.

The MNA result remains an object with separate nullable screening/global/total outcomes. Extent changes preserve all A–R answers and measurements. Local raw inputs/errors remain outside the DTO and must block save/preview rather than submit a hidden previous value. The dedicated snapshot retains partial G–R/K data, measurement dates/provenance and authoritative sex/age, without changing published snapshot types. Exact fields/enums/labels/source notices are in the frozen DTO.

## Runtime and preserved baseline

The root dependency path is a junction to `C:/Workspace/ClinicOSHouse-worktrees/quality-loop-20260829/node_modules`, treated as read-only. Only local `frontend/node_modules/.tmp` and `.vite-temp` caches were created. No install, dependency regeneration, package or lockfile change occurred. Runtime inspection resolved Node v26.3.0, TypeScript 6.0.3, tsx 4.23.1, React/React DOM 19.2.5, Vite 8.2.2 and Prettier 3.9.5. Package presence does not claim an application test/build pass.

`baseline-source-manifest.json` binds 521 tracked frontend/source/config inputs and the preserved dirty PowerShell files to source-state ID `bb2890362d3d3ce078087d6f7ea8a691ea30b6ce32873b939e01b59394cbe913`. The application diff is empty.

The PO12 mobile sticky-identity fix in `AssessmentWorkspace.css` is part of this baseline and is protected at SHA256 `b36eca8bc9fd9f584410f7ff3a3469f812f197d6e96a435659902adc5ee91be1`. Preserve body clip/visible overflow, visible main-content overflow, the 64px topbar offset and matching focus scroll margin. Do not restore the earlier PO12 CSS hash from historical evidence.

Root will preview PO13 from a static build because Vite dev middleware previously deadlocked during scanning. This worker owns no preview port and starts no middleware or application server.

## After root GO

Activate a scoped application claim before editing. Implement the agreed MNA definitions, strict DTO validation, progressive form/summary and shared assessment/archive branches, together with focused regressions. Preserve PO10–12 behavior and the protected CSS/scripts. Use direct Node test-runner commands from `frontend` for focused tests; the npm test wrapper does not forward file arguments. Bind tests, build, lint comparison and source/bundle secret scan to the candidate source state. Root owns integrated HTTP/PostgreSQL/browser/PDF checks and publication.

No application test/build, runtime service, clinical validation, benchmark, commit or deployment is claimed by this preparation.
